#ifndef ARRAY_DINAMICO_H
#define ARRAY_DINAMICO_H

#include <iostream>

// --- Prototipos de Funciones Modulares ---

// 1. Ordenamiento por Burbuja (Bubble Sort)
// Recibe un puntero al array dinámico y el tamaño N.
void sortDynamicArray(int* datos, int size); 

// 2. Función auxiliar para mostrar el arreglo dinámico.
void printArray(int* arr, int n);

#endif // ARRAY_DINAMICO_H